apiVersion: v1
kind: Secret
metadata:
  name: notification-postgres-credentials
  namespace: mindlet
  labels:
    mindlet.io/workload: notification
    app.kubernetes.io/part-of: mindlet
type: Opaque
stringData:
  POSTGRES_USER: mindlet
  POSTGRES_PASSWORD: mindlet
  POSTGRES_DB: mindlet_notification
---
apiVersion: v1
kind: Service
metadata:
  name: notification-postgres
  namespace: mindlet
  labels:
    mindlet.io/workload: notification
    app.kubernetes.io/part-of: mindlet
spec:
  clusterIP: None
  ports:
    - port: 5432
      targetPort: 5432
      name: postgres
  selector:
    app.kubernetes.io/component: postgres
    mindlet.io/workload: notification
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: notification-postgres
  namespace: mindlet
  labels:
    mindlet.io/workload: notification
    app.kubernetes.io/part-of: mindlet
spec:
  serviceName: notification-postgres
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/component: postgres
      mindlet.io/workload: notification
  template:
    metadata:
      labels:
        app.kubernetes.io/component: postgres
        mindlet.io/workload: notification
        app.kubernetes.io/part-of: mindlet
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          ports:
            - containerPort: 5432
          envFrom:
            - secretRef:
                name: notification-postgres-credentials
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
              subPath: pgdata
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: "1"
              memory: 512Mi
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 5Gi
